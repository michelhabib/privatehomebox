graph LR
    subgraph ServerProcess["Server process"]
        HTTP["HTTP Server<br/>(API — always on)"]
        CM["Channel Manager"]
        CommM["Communication Manager"]
        AM["Agent Manager"]
        Admin["Admin UI<br/>(NiceGUI — optional)"]
        Tools(["Tools"])
    end

    CLI["CLI"] -->|"direct import"| Tools
    HTTP -->|invoke| Tools
    AM -->|invoke| Tools
    Admin -->|"direct import"| Tools
    Admin -.->|"read queues"| CommM
    Admin -.->|"read status"| CM
